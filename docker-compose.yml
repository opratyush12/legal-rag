services:
  # ── FastAPI backend ──────────────────────────────────────────────────────────
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: lexsearch-backend
    restart: unless-stopped
    env_file: ./backend/.env
    volumes:
      # Persist FAISS index between restarts
      - ./backend/index_store:/app/index_store
      # Mount PDF directory (local dev — swap for S3 in prod)
      - type: bind
        source: ${PDF_LOCAL_DIR:-./pdfs}
        target: /data/pdfs
        read_only: true
    environment:
      - PDF_LOCAL_DIR=/data/pdfs
      - INDEX_DIR=/app/index_store
    # Backend only exposed internally — frontend proxies via nginx
    expose:
      - "8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: "1.0"

  # ── React frontend (Nginx) ───────────────────────────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: lexsearch-frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      backend:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: "0.25"
